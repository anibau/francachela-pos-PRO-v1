import { Repository } from 'typeorm';
import { Producto } from '../../entities/producto.entity';
import { MovimientoInventario } from '../../entities/movimiento-inventario.entity';
import { CreateProductoDto } from './dto/create-producto.dto';
import { UpdateProductoDto } from './dto/update-producto.dto';
import { PaginationDto } from '../../common/dto/pagination.dto';
import { PaginatedResult } from '../../common/interfaces/paginated-result.interface';
import { ActualizarStockDto } from './dto/actualizar-stock.dto';
export declare class ProductosService {
    private productoRepository;
    private movimientoRepository;
    constructor(productoRepository: Repository<Producto>, movimientoRepository: Repository<MovimientoInventario>);
    create(createProductoDto: CreateProductoDto, cajero: string): Promise<Producto>;
    findAll(paginationDto: PaginationDto): Promise<PaginatedResult<Producto>>;
    findById(id: number): Promise<Producto>;
    findByCodigoBarra(codigoBarra: string): Promise<Producto>;
    search(query: string, paginationDto: PaginationDto): Promise<PaginatedResult<Producto>>;
    findByCategoria(categoria: string, paginationDto: PaginationDto): Promise<PaginatedResult<Producto>>;
    findStockBajo(): Promise<Producto[]>;
    getCategorias(): Promise<string[]>;
    update(id: number, updateProductoDto: UpdateProductoDto): Promise<Producto>;
    actualizarStock(id: number, actualizarStockDto: ActualizarStockDto, cajero: string): Promise<Producto>;
    descontarStock(codigoBarra: string, cantidad: number, cajero: string, ventaId?: number): Promise<void>;
    devolverStock(codigoBarra: string, cantidad: number, cajero: string, ventaId?: number): Promise<void>;
    remove(id: number): Promise<void>;
    activate(id: number): Promise<Producto>;
    private registrarMovimiento;
    getMovimientosInventario(paginationDto: PaginationDto): Promise<PaginatedResult<MovimientoInventario>>;
    getMovimientosByProducto(codigoBarra: string, paginationDto: PaginationDto): Promise<PaginatedResult<MovimientoInventario>>;
}
